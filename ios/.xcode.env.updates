#!/bin/sh

# Keep Metro-based behavior on simulator debug, but embed JS for real-device debug.
case "$CONFIGURATION" in
  *Debug*)
    if [ "$PLATFORM_NAME" = "iphoneos" ]; then
      export SKIP_BUNDLING=0
    fi
    ;;
esac
